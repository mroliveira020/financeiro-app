# Configuração padrão para os scripts de garimpo.
# Copie para garimpo/config.yaml e ajuste conforme necessário.

data:
  input: data/input/base.xlsx
  output_dir: data/output
  # Modo de entrada: excel (planilha única), csv (arquivos por UF), auto (pergunta se CSVs existirem)
  input_mode: auto
  # Padrão de arquivos CSV por UF (se usado input_mode=csv ou auto)
  csv_pattern: data/input/Lista_imoveis_*.csv

principal:
  # Lista de UFs que devem ser buscadas no script principal
  ufs: ["GO", "SC"]
  # Nome do arquivo de saída combinado
  output_filename: "output.xlsx"
  # Quando verdadeiro, ignora imóveis já registrados no arquivo de saída
  skip_existing: true
  # Define a cada quantos registros os dados devem ser persistidos (0 = somente ao final)
  chunk_size: 0

extrajudicial:
  # Tipos de venda aceitos para coleta
  venda_tipos: ["Venda Direta", "Venda Direta Online"]
  output_filename: "output.xlsx"
  output_financiado_filename: "output_financiado.xlsx"

http:
  timeout: 5
  # Sobrescreva cabeçalhos HTTP apenas se necessário (default definido no código)
  headers: {}
  # Informe cookies quando a CAIXA exigir sessão autenticada
  cookies: {}

email:
  enabled: false
  smtp_server: ""
  smtp_port: 587
  use_tls: true
  username: ""
  password: ""
  from_address: ""

supabase:
  enabled: true
  url: "https://thmekudlkuwjuddkyhpi.supabase.co"
  anon_key: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InRobWVrdWRsa3V3anVkZGt5aHBpIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Mzk3NDM4MTUsImV4cCI6MjA1NTMxOTgxNX0.8tWYoeF3krCzYDJX6jony_1hEAacS4F-tOtbNFGy6as"
  service_role_key: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InRobWVrdWRsa3V3anVkZGt5aHBpIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTczOTc0MzgxNSwiZXhwIjoyMDU1MzE5ODE1fQ.AZogBNPM-O7FrD0oSa7wfLY0dSczAfbR3jJchOQZ9dQ"
  chunk_size: 500
  timeout: 10
  retry:
    attempts: 3
    backoff_seconds: 2
  error_log: data/output/erros_supabase.csv
